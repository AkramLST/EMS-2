import { redirect } from "next/navigation";

export default function Register() {
  return null;
}

/*
// Previous public registration page implementation has been intentionally
// disabled. Keeping the original code here for reference in case it needs to
// be restored in the future.

import RegisterPage from "@/components/auth/RegisterPage";

export default function Register() {
  return <RegisterPage />;
}
*/
